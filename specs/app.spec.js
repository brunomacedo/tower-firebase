describe('App', () => {
  describe('Method A', () => {
    context('Case 1', () => {
      it('Should happen somenthing1.', () => {
        // throw new Error('Just an error');
      });
    });

    context('Case 2', () => {
      it('Should happen somenthing2.', () => {
        // Do something.
      });

      it('Should happen somenthing3.', () => {
        // Do something.
      });
    });
  });

  describe('Method B', () => {
    context('Case 1', () => {
      it('Should happen somenthing1.', () => {
        // Do something.
      });
    });

    context('Case 2', () => {
      it('Should happen somenthing2.', () => {
        // Do something.
      });

      it('Should happen somenthing3.', () => {
        // Do something.
      });
    });
  });
});
